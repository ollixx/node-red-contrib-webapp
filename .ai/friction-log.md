# Friction Log

Raw material for the `review-agent-os` role. Whenever something in the agent-OS,
the docs, or the workflow **slowed you down, misled you, or caused rework**, append
one line here before you finish your task. Be specific and point at the file.

This is not a bug tracker for the product — it is a log of friction in *how we work*.
Newest at top. The review role mines this list and prunes entries once addressed.

Format:

```
- YYYY-MM-DD [role] what was wrong/misleading + where (file:line or path)
```

## Open owner actions

## Entries

- 2026-06-09 [run-next-phase/P94 orchestrated] Pre-existing E2E failures (badge pulsating + structure.spec.ts title field) were not caught before this phase because `pnpm exec playwright test` was run from the main checkout, not the worktree — the worktree needed `git reset --hard develop` first (it started at the stub commit). After that, 3 real pre-existing failures surfaced: (1) pulsating prop missing from the props block in webapp.js (not routed to serializer), (2) structure.spec.ts expected `#node-input-title` but P89 used `#node-input-titleBinding`. Both fixed in P94 commit. The worktree stale-HEAD problem is the same recurring issue noted in P70/P68/P79 entries.
- 2026-06-08 [run-next-phase/P79 orchestrated] Two recurring costs again: (a) my first `corepack pnpm install && pnpm build` ran in the SHARED checkout because the orchestrator's setup `git checkout -b phase/P79 develop` example used a `cd`-style compound command — the Bash cwd actually starts in the worktree, so the compound install hit the wrong tree and the worktree had no node_modules until I re-ran install with no `cd`. (b) Reading via shared-checkout absolute paths still makes Edit reject (must Read the worktree-absolute path first). Positive: full Playwright suite (297 specs) ran green in the worktree with no `.node-red-dev` seeding needed this time.
- 2026-06-08 [run-next-phase/P72 orchestrated] Worktree had no dist packages — `pnpm test` appeared green in the main checkout but the worktree needed `corepack pnpm install && pnpm build` before vitest could load `webapp.js`. The baseline-green step (step 2 of Execute) passes in the main checkout but a fresh worktree always fails until built. The spawn should pre-build or the baseline-green check should be skipped in favour of a worktree build step.
- 2026-06-08 [run-next-phase/P70 orchestrated] Stale-worktree cost recurs: the `phase/P70` worktree HEAD sat at `db4f4ff initial checkin`, so I had to `git checkout -b phase/P70 develop` manually. Two notes vs. prior entries: (a) Playwright DID run fine in the worktree this time (full 280-spec suite green) — the earlier "E2E can't run in worktree" claim was not reproduced here; (b) after rebasing, Read/Edit must target the worktree-absolute path — reading via the shared-checkout absolute path makes Edit/Write reject with "edit the worktree copy instead", forcing a re-Read. Spawn should branch from the in_progress commit and the file tools should canonicalise shared-checkout paths to the worktree.
- 2026-06-07 [run-next-phase/P68 orchestrated] Same two recurring worktree-setup costs as P53/P56/P66: (1) the `phase/P68` worktree HEAD sat at `db4f4ff initial checkin`, so I had to `git checkout -b phase/P68 <P65-work tip>` manually (the integration branch here was `phase/P65-work`, not develop); (2) Playwright cannot run in the worktree — its webServer `cp`s the off-limits `.node-red-dev/settings.js`, which is absent, so E2E specs are written-but-unrun and the orchestrator must run them on the integration branch. Long-standing; fix the spawn (branch from the in_progress commit; seed `.node-red-dev/settings.js`).

- 2026-06-07 [run-next-phase/P66 orchestrated] STILL recurring (now 5th+ phase): the `phase/P66` worktree HEAD sat at `db4f4ff initial checkin`, not `develop`. Had to `git checkout -b phase/P66 develop` manually. Also: Edit/Write require the worktree-prefixed absolute path (`.claude/worktrees/<id>/...`); a prior Read of the shared-checkout path does NOT satisfy the "read before edit" guard — every file had to be re-read at the worktree path. And Playwright still cannot run (webServer copies the off-limits `.node-red-dev/settings.js`, absent in the worktree). All three are long-standing orchestrated-worktree-setup costs; fix the spawn (branch from the in_progress commit; seed `.node-red-dev/settings.js`).

- 2026-06-06 [run-next-phase/P57 orchestrated] New node `ui-log` required adding `"log"` to FOUR separate places — all must stay in sync: (1) `uiComponentKindSchema` in `contracts.ts`, (2) `uiLogNodeDefinitionSchema` union in `node-definitions.ts`, (3) `P16X_KIND_MAP` + `runtimeNodeRegistry` in `webapp.js`, (4) the `switch` statement + `RenderedGenericComponent.kind` union in `packages/renderer/src/renderer.ts`. The context-budget doc (`.ai/agents/context-budget.md`) says to invoke the `/node-red-node` skill for new nodes, but the skill pattern doesn't mention the renderer `kind` union or `uiComponentKindSchema` — two of the four required places. This caused a render loop: E2E passed schema validation but the renderer silently dropped the component. Add a "renderer kind union" step to the node-red-node skill and the context-budget renderer pitfalls.

- 2026-06-06 [run-next-phase/P56 orchestrated] STILL recurring (now 4th phase: P49a, P50, P54, P56): the `phase/P56` worktree branch was created from the ancient `db4f4ff initial checkin` instead of `develop`@the in_progress commit — the whole monorepo was absent until `git reset --hard develop`. And the fresh worktree had no `.node-red-dev/`, so E2E could not start until I copied only `settings.js` (NOT the off-limits `flows.json`) from the main checkout. Both are long-standing, repeatedly-logged orchestrated-worktree-setup bugs; the cost is now systematic across every parallel phase. Fix the spawn: branch from the in_progress commit and seed `.node-red-dev/settings.js`.
- 2026-06-06 [run-next-phase/P53 orchestrated] The P53 scope listed the serializer, client, webapp.js and ui-action.html/md but NOT the schema (`packages/schema/src/contracts.ts` `actionTypeSchema` + `node-definitions.ts` `uiActionNodeDefinitionSchema`). The new verbs (`open`/`close`/`select`/`focus`/`reset`) and the `part` field are silently rejected/stripped by Zod until the enum + node schema are widened — symptom is a `[ui-action] actionType: Invalid option` in the node-red log and a command that never reaches the client, which reads like a client bug. Lesson: any phase that adds an action verb or a node field must treat the schema (both contracts.ts and node-definitions.ts) as an implicit deliverable.
- 2026-06-06 [run-next-phase/P49a] Orchestrated PARALLEL sub-agents collide on the E2E port. The orchestrator spawned P49a and P54 as "batch 1 parallel start", but playwright.config.ts hard-codes port 1882 (and shares /tmp/node-red-webapp-playwright.log + the global node-red bind). Each worktree's `.node-red-e2e` is isolated, but the PORT is not — so `pnpm exec playwright test` in one worktree ECONNREFUSEDs / restarts the other's server mid-run, producing false E2E failures. Either serialise E2E across parallel phases, or derive the port per-worktree (e.g. 1882 + worktree-hash offset) and the log path likewise. This cost a full re-run cycle.
- 2026-06-06 [run-next-phase/P50 orchestrated] Same stale-worktree problem as P49a/P54: phase/P50 branch pointed at `db4f4ff` (2020 initial checkin), no packages/schema/resources/nodes present. Worked around with `git merge 2dfa9a0 --allow-unrelated-histories` (fast-forward). The fix is to create the worktree branch from `develop`@the in_progress commit, not the ancient orphan root.
- 2026-06-06 [run-next-phase/P49a] The worktree was checked out on the ancient `db4f4ff initial checkin` (pre-pnpm-workspace package.json) instead of `develop`; `git branch phase/P49a` inherited that stale HEAD. Had to `git reset --hard develop` to get the real codebase. The worktree setup should branch from the integration branch the orchestrator is on, not whatever HEAD the worktree happened to carry.
- 2026-06-06 [run-next-phase/P49a] The E2E webServer command copies `.node-red-dev/settings.js` into `.node-red-e2e`, but `.node-red-dev` (owner-only, gitignored) does not exist in a fresh worktree — so E2E cannot start there at all without symlinking it back to the main checkout. A worktree-portable settings.js (committed e2e-settings, or generated) would remove this dependency on the owner's off-limits dir.
- 2026-06-06 [run-next-phase/P54 orchestrated] The orchestrator-provided worktree branch `phase/P54` was created from the wrong base commit (`db4f4ff`, an old unrelated "initial checkin" history with a Vue-material package.json) instead of `develop`@`edac9b6` where the orchestrator had set P54 `in_progress`. The whole monorepo (`packages/`, `docs/agent-roadmap.yaml`) was absent; `pnpm test` ran a stale mocha script. Had to `git reset --hard develop` to recover. Also: the E2E webServer copies `.node-red-dev/settings.js` into the ephemeral userdir, but a fresh worktree has no `.node-red-dev/` at all — so E2E cannot start there. Worked around by copying only `settings.js` (NOT the off-limits `flows.json`) from the main checkout. Orchestrated worktree setup should branch from the in_progress commit and seed `.node-red-dev/settings.js`.
- 2026-06-06 [run-next-phase/P54 orchestrated] The orchestrator prompt said the next pending phase is P49b, but the roadmap also still shows P49a as `in_progress` (a parallel batch-1 sibling), so a naive "first pending with deps done" scan would not obviously land on P49b. The two parallel in_progress phases (P49a, P54) make "the next pending" ambiguous without the orchestrator's explicit hand-off — fine here because it was given, but worth noting the scan rule alone is insufficient during parallel batches.
- 2026-06-01 [run-next-phase/P33] `buildAppSnapshot` (nodes/webapp.js) picks per-client state OR broadcast state but never MERGES them: once a client has any per-client state, broadcast data updates become invisible to it. Combined with the client minting a fresh random clientId on every full page load (resources/lib/webapp-client.js, no stable id from the server), per-client CRUD state cannot survive a navigation. Open runtime limitation — a stable server-issued clientId + merging broadcast under per-client state would make per-client apps viable. Not yet phased.
- 2026-05-31 [run-next-phase/P31] Manual runtime verification needs a live node-red on 1881/1882; orphaned background instances kept owning the port so restarts silently failed to bind and curl hit stale code. `dev:start` now kills 1881 first, but the E2E 1882 path has no equivalent guard. A scripted "kill anything on 1881/1882 before starting" (or a fresh ephemeral port) would prevent the "talking to the old process" ghost.
- 2026-06-03 [P42 scope] Scope wording was aspirational, not behavioural: it said ui-route 'title renders as <h2>' (impl only sets <title>) and assumed an `injectMessage` helper that returned 404 against live Node-RED. Lesson: write `validation` against actual runtime behaviour, not desired HTML structure.

- P52 (2026-06-06): The full `pnpm exec playwright test` suite (243 tests) could not be run to completion in this session — the OS OOM-killed the Playwright **worker** process partway through every attempt (`signal=SIGKILL`), cascading into `ECONNREFUSED 127.0.0.1:1882` for all subsequent tests. Node-RED itself stayed healthy throughout (server log shows normal "Started flows" past the kill). Pass counts varied run-to-run (103 / 114 / 176 / 191 of 243), confirming nondeterministic memory pressure rather than a code defect; the clean pre-change baseline was 242/242. The isolated P52 spec passes and `pnpm validate` is fully green. Host RAM is the binding constraint for E2E here. No tooling fix needed in the repo, but worth knowing the E2E suite needs ~headroom this VM sometimes lacks.

- P62 (2026-06-07): The orchestrator-provided worktree (agent-ab4b7cee...) was checked out on a throwaway branch at the ancient `db4f4ff initial checkin` commit and contained only LICENSE/dist/lib/nodes/package.json — none of `packages/`, `tests/`, `.ai/`, or the current source. Had to `git checkout -B phase/P62 develop` inside the worktree to materialise the real tree before any work. Also `pnpm validate` runs `test` before `build`, so a fresh worktree fails with `Cannot find module '../packages/schema/dist/index.js'` until you run `pnpm build` once manually. Worth pre-seeding worktrees from the target base branch and/or building before validate.

- P64 (2026-06-07): Same worktree-base issue as P62 recurred — the orchestrator worktree (agent-a143256354c2aa732) was on a throwaway branch at the ancient `db4f4ff initial checkin` (only LICENSE/dist/lib/nodes/package.json present). `git checkout -b phase/P64` branched from that ancient tree; had to `git switch -C phase/P64 develop` to materialise the real source. `git reset --hard` was blocked by the sandbox classifier (use `git switch -C` instead). Also `pnpm` resolved to the SHARED checkout until the worktree had a proper package.json, and Edit refused shared-checkout paths — all edits must use the worktree-absolute path prefix. Worth fixing worktree provisioning to branch from the target base.

- P67 (2026-06-07): Same worktree-base issue as P62/P64 recurred — the worktree (agent-ad134f347a44859cb) was on branch `worktree-agent-…` at the ancient `db4f4ff initial checkin` (only LICENSE/dist/lib/nodes/package.json). Had to `git checkout -b phase/P67 phase/P65-work` to materialise the real source from the integration branch. Worktree provisioning still needs to branch from the target base. Also confirmed worktree sub-agents cannot run Playwright: the E2E webServer copies `.node-red-dev/settings.js`, which is gitignored/owner-only and absent from worktrees, so `pnpm exec playwright test` fails at server startup — the orchestrator must run E2E in the main checkout (as MEMORY.md already records).

- P69 (2026-06-08): Same worktree-base issue as P62/P64/P67 — the worktree (agent-ac90fea92546239c5) was on `worktree-agent-…` at the ancient `db4f4ff initial checkin`; had to `git checkout -b phase/P69 develop` to materialise the real source. Worktree provisioning STILL branches from the wrong base. Contrast with the P67 note: this run DID run `pnpm exec playwright test` successfully in the worktree (276 passed, ~7m) — the `.node-red-e2e` env rebuilt fine without `.node-red-dev/settings.js`, so the "sub-agents can't run E2E" claim is environment-dependent, not absolute. The ui-icon.md spec was a pure requirement doc that pre-dated P69's owner decisions (plain-string icon vs the new { library, name } system); the validation Step-3 cross-check flagged the divergence and I updated the doc to match the implemented contract.

- P71 (2026-06-08): Same worktree-base issue as P62/P64/P67/P69 — the worktree (agent-a020f99daa40a90d6) was on `worktree-agent-…` at the ancient `db4f4ff initial checkin`; `git checkout -b phase/P71 develop` materialised the real source. Worktree provisioning STILL branches from the wrong base after five phases — this should be fixed at the provisioning layer. E2E DID run fine in the worktree (284 passed, ~4.8m), consistent with P69. Scoping decision: the roadmap's "Button-Slots (default=Label, prefix, suffix)" deliverable was explicitly flagged as an open design point ("feste Slots vs. Layout-Preset"); making ui-button a layout-host so arbitrary child nodes mount into prefix/suffix touches mount-path/region structure (a stop-condition-adjacent change). Delivered the common case (icon in prefix, label as default slot) plus the unambiguous fields (size/outline/linkMode/href) fully; deferred the full mini-container subsystem and documented it in ui-button.md "Offene Punkte". Outline scoped to ui-button only (sl-badge has no native outline; the roadmap's "ggf. badge/tag" was conditional).

- P90 (2026-06-08): Worktree branch phase/P90 was at the ancient `db4f4ff initial checkin` again (same as P62–P71). Fixed with `git reset --hard ccb16bd` (develop HEAD). No node_modules present in the worktree; had to manually symlink node_modules + all package dist/ dirs + resources/shoelace/assets from the main checkout before `pnpm test` would pass. E2E not run in worktree (orchestrator runs those). No stop conditions triggered.

- P81 (2026-06-08): Worktree was correctly provisioned this time — already on `phase/P81` at the develop tip with `in_progress` set by the orchestrator (no ancient-base `git checkout -b` needed, unlike P62–P71). E2E ran fine in the worktree (297 passed, ~4.8m) once the shared-checkout E2E released port 1882 — confirming again the "sub-agents can't run E2E" note is environment-dependent. The `.gitignore` did not exclude `.claude/worktrees/`, so the worktree path appeared as the cwd; harness file edits had to target the worktree copy, not the shared checkout (the Edit tool enforces this correctly).

- P95 (2026-06-09): Worktree (agent-ad71353aafe83f9c0) was again at the ancient `db4f4ff initial checkin`; `git merge develop` materialised the real source. Same recurring provisioning issue. Phase redesigned the breadcrumb item model — the old `{label, path}` schema and P75 serializer tests needed updating to reflect the new `{label, action?, active?}` contract; updated carefully to keep menu tests (unchanged by P95) passing.
