# Friction Log

Raw material for the `review-agent-os` role. Whenever something in the agent-OS,
the docs, or the workflow **slowed you down, misled you, or caused rework**, append
one line here before you finish your task. Be specific and point at the file.

This is not a bug tracker for the product — it is a log of friction in *how we work*.
Newest at top. The review role mines this list and prunes entries once addressed.

Format:

```
- YYYY-MM-DD [role] what was wrong/misleading + where (file:line or path)
```

## Open owner actions

## Entries

- 2026-06-12 [phase/P158] The `@integration` customers-crud suite (`tests/e2e/customers-crud.spec.ts`) is RED on the clean P158 base (SHA 73e1148): the 7 navigate-driven CRUD tests fail with `toHaveURL(/\/customers$/)` timing out on the app root — the documented pre-existing navigate-runtime breakage (friction entry 2026-06-10 [orchestrator] FALSE-GREEN CASCADE + the P118 NR-v3 JSONata-async note: navigate lands on the app root on this host's Node-RED). Verified by stashing all P158 changes and re-running: identical 7 navigate failures on base, so NOT a P158 regression. P158's OWN deliverable is green: the table-render integration test ("LIST: the seeded customers appear in the table") passes, as do all 9 ui-table composite specs (incl. new store-array rows + rowsPath migration) and the view editor specs. Also: `pnpm gen:example` still carries the long-standing badge drift (`severity`→`variant`/`displayType:"rounded"`) — regenerating to satisfy the parity guard pulls that unrelated field rename into the P158 commit (same friction as 2026-06-09 [run-next-phase/P106]). The owner should regen-and-commit the badge drift on develop independently so field-typing phases stop carrying it.
- 2026-06-12 [phase/P154] The spec `docs/nodes/navigation/ui-pagination.md` used the *compiled* field names `page`/`totalPages` (the schema-definition keys) for the editor fields, while the P154 contract's canonical editor field names are `currentPage`/`total` (P137/P153-style typedInputs). Had to reconcile spec ↔ implementation: the editor/config layer now uses `currentPage`/`total`, which webapp.js mapConfig compiles to the schema `page`/`totalPages` bindings. LESSON: per-node spec "Felder" tables should name the *editor* field (what the user sees + what the editor persists), not the internal compiled binding key — otherwise the field-typing-wave phases each have to fix the table. Also: ui-pagination's runtime mapping lives in `nodes/webapp.js` (mapConfig + the p16Kind component builder), NOT `packages/runtime/src/node-set.ts` (which only assembles a hard-coded subset of component types) — the context-budget pointer for view nodes should call this out.

- 2026-06-10 [orchestrator/batch2] `tests/e2e/nodes/structure/p112-route-lifecycle-on-arrival.spec.ts:110` ('refresh re-fires onEnter') flaked once in a full-suite run (1 failed/474 passed) but passes deterministically in isolation (2/2, exit=0) — it is timing-sensitive (loadId/SSE-reconnect + 3s grace under full-suite load), not a regression from the ui-textarea/slider/radio batch (those touch nothing in route lifecycle). Candidate for stabilisation (grace-period/wait tuning) as its own fix-bug if it recurs.

- 2026-06-10 [orchestrator/P127] (a) A P127 sub-agent committed an OUT-OF-SCOPE change (reactive-error-dedup in webapp.js + a runtime test, commit 2db3ab8) on top of its node work — scope creep beyond the ui-radio package. Caught by inspecting `git log phase/P127 ^develop` before merge; merged ONLY the node commit (54bf7fb) via `git merge --no-ff <sha>`, preserved the stray commit on branch salvage/reactive-error-dedup for separate evaluation. LESSON: before merging a phase branch, `git log phase/<id> ^develop --stat` and confirm every commit is in-scope; merge by commit-SHA, not branch tip, if a stray commit rode along. (b) The orchestrator's MAIN checkout was found sitting on `phase/P127` (not develop) when I went to merge — a worktree-isolation leak; `git merge` reported 'Already up to date' because HEAD was the phase branch. LESSON: before any merge/bookkeeping, assert `git branch --show-current` == develop (or `git switch develop`).

- 2026-06-10 [orchestrator/P122] WORKTREE BRANCHED FROM STALE develop (not the orphan db4f4ff this time): the worktree's local `develop` ref lagged ~8 commits behind the real tip — critically it predated the P113 code merge. The sub-agent's worktree-sanity step (`HEAD == develop` else `git switch -C phase/<id> develop`) passed because it compared against the STALE local develop, so it branched from a develop WITHOUT P113's canonical `valueBindingTypes`/`readValueBinding`/`applyValueBinding` helpers — and the agent dutifully RE-IMPLEMENTED them (140-line editor-common.js diff), defeating P113's single-source-of-truth. Merging would have duplicated/conflicted. Discarded + re-spawned. FIX: the worktree-sanity step must verify a KNOWN-RECENT commit is an ancestor (orchestrator passes the develop SHA + a sentinel like the latest dependency's merge commit; sub-agent runs `git merge-base --is-ancestor <dep-sha> HEAD` and STOPS if false), not just `HEAD==develop`. Also: a phase that CONSUMES a helper must grep that the helper already exists before starting; if absent → stale base, STOP, do not reimplement.

- 2026-06-10 [orchestrator] STALE-BUILD FALSE-RED (mirror of the tail false-green): after merging phases I ran the authoritative develop E2E WITHOUT a prior `pnpm build`. A merge brings compiled-package SOURCE (packages/schema|runtime|renderer/src) onto develop, but the checkout's `packages/*/dist` stays stale until rebuilt. `nodes/webapp.js` loads from dist at runtime, so P118's new navigate schema fields were silently stripped by the OLD compiled schema → navigate landed on the app root → 6 E2E false-red. I reopened P118 and spawned a fix sub-agent before discovering (via the sub-agent's own worktree-sanity rebuild) that `pnpm build` on develop makes all 6 green. The 4 picker failures were REAL (editor-common.js is raw-served, build-independent), so the mixed result is the tell: build-dependent specs flip green on rebuild, raw-served ones don't. RULE (now in validation.md + orchestrator close-out): ALWAYS `pnpm build` on the develop checkout before the authoritative E2E; if a spec is red, re-run after a clean build before treating it as a defect.

- 2026-06-10 [orchestrator] FALSE-GREEN CASCADE: I verified the post-merge full E2E for P114, P117 and P118 with `pnpm exec playwright test 2>&1 | tail -N`. The pipe sets the shell exit code to tail's (0) and `tail -N` cuts off Playwright's `N failed` header (printed ABOVE the trailing failed-test list), so each run *looked* green ("405/408/410 passed") while real failures scrolled off the top. Net result: three phases were closed `done` on masked-green, and 10 real regressions accumulated on develop undetected until a P120 test-count cross-check exposed it. Confirmed via the run-start baseline (task b1t4lbnqj, read from the output FILE not a pipe) which was clean (397 passed, 2 known ui-text staleness). The 10: 6 navigate-runtime (P118 — navigate no longer reaches the target route, lands on app root; even the pre-existing P46 ui-action/ui-navigation navigate tests fail) + 4 editor mount-picker (P114/P117 — `.webapp-node-picker-row` never appears in mount dialog). HARD RULE for this OS: the authoritative close-out E2E must NEVER be piped through tail/head. Capture to a file (`> run.log 2>&1; echo exit=$?`) and assert exit==0 AND grep -c ' failed ' ==0. Candidate for validation.md + the orchestrator prompt's close-out step.

- 2026-06-10 [orchestrator/P120] VERIFICATION TRAP: ran the authoritative post-merge E2E as `pnpm exec playwright test 2>&1 | tail -6` — the pipe makes the shell exit code that of `tail` (0), masking Playwright's non-zero exit, and `tail -6` cut off the `N failed` header (it prints ABOVE the trailing failed-test list), leaving only `408 passed` visible. Closed P120 as done on a false-green. Caught it via a test-count cross-check (P120 adds 5 tests but total stayed 408 = the P118 baseline). RULE: never pipe the close-out E2E run through tail/head; capture full output to a file (`> run.log 2>&1; echo exit=$?`) and grep for `failed`/exit. The 5 dual-path-coding tests actually fail because the feature never activates in the real editor (auto-registration via RED.events does not fire).
- 2026-06-10 [orchestrator/P115] Sub-agent (opus) wrote literal NUL bytes (0x00) into string literals in packages/renderer/src/reactive-expression.ts — git/grep treated the .ts as binary; fixed post-merge by replacing with \u0000 escapes. Worth a validation.md check: 'git diff --stat must show no unexpected Bin entries'.

- 2026-06-09 [run-next-phase/P106] Two traps cost rework: (a) `status` is a RESERVED Node-RED node property (the runtime status indicator object) — using it as a ui-app config key made `this.status` an object and the editor select blank; renamed the config key to `deployMode` (label stays "Status"). A context-budget/node-skill pitfall note ("never name a node config field `status`/`name`/`id`/`type`/`wires`/`x`/`y`/`z` — they are reserved") would have saved a debug cycle. (b) The P37 deploy-reload was **dead code**: the `flows:started` listener lived only in `registerWebappNodes()` (the legacy factory), but Node-RED invokes nodes via `registerWebappNodes.registerNodeType` per type — the factory is never called, so the listener never registered. The comment at `nodes/webapp.js` registerNodeType already says "not the legacy registerWebappNodes factory" but nothing flagged that the factory still held a live-looking event hook. Moved the hook into `registerEndpoints` (the path that runs) with a once-guard.
- 2026-06-09 [run-next-phase/P106] `pnpm gen:example` regenerates `examples/customers-crud/flow.json` with an UNRELATED pre-existing drift (a badge fixture `variant:"status"`/`severity` → `displayType:"rounded"`/`variant` rename from an earlier phase) — running it as AGENTS.md rule 5 demands would pull that drift into an unrelated phase's commit. Reverted and skipped (my added field is optional+defaulted, so the example is unaffected). The committed flow.json is stale vs the typed fixture; worth a one-shot regen by the owner.
- 2026-06-09 [review-agent-os] Pruned this log: removed the 15+ duplicate "worktree HEAD sat at the ancient db4f4ff orphan base instead of develop" entries (P49a/P50/P53/P54/P56/P62/P64/P67/P69/P71/P90/P95/P96/P98/P99) now addressed by the new **worktree-sanity step** in run-next-phase Orchestrated mode; removed the P57 "new node kind needs 4 synced places" entry (now a context-budget pitfall) and the P42 "write validation against runtime behaviour, not desired HTML" entry (now covered by the observable-`acceptance` detail bar). **Remaining un-fixed worktree quirks** (harness-level, prompt can only partially mitigate): (a) Edit/Write require the worktree-absolute path even after Reading the shared-checkout path; (b) a sparse worktree may lack `resources/`, so unit tests that `require('../../resources/lib/…')` fail until built/copied; (c) running Playwright inside a worktree is environment-dependent (works some runs, fails others on `.node-red-dev/settings.js`) — the orchestrator runs full E2E on develop after merge regardless. Worth an owner report to the harness team if it persists.
- 2026-06-06 [run-next-phase/P49a + P31] E2E port is not isolated across parallel/concurrent runs: `playwright.config.ts` hard-codes port 1882 and shares `/tmp/node-red-webapp-playwright.log`, so two parallel worktree phases (or a stray background server) ECONNREFUSE / restart each other's server mid-run → false failures. `dev:start` kills 1881 first, but the E2E 1882 path has no equivalent guard. Fix: serialise E2E across parallel phases, or derive port+log per worktree (e.g. 1882 + hash offset).
- 2026-06-06 [run-next-phase/P52] The full Playwright suite is RAM-bound on this VM: the worker is OOM-killed (SIGKILL) partway through some runs, cascading into ECONNREFUSED for the rest; pass counts vary run-to-run (nondeterministic memory pressure, not a code defect). No repo fix, but E2E sometimes needs headroom this host lacks — re-run or run in shards when it happens.
- 2026-06-06 [run-next-phase/P54] During parallel batches, "the first pending phase whose deps are done" is ambiguous when two siblings are simultaneously `in_progress` — the scan rule alone is insufficient; the orchestrator's explicit hand-off is load-bearing. Worth making the orchestrator state the chosen phase id explicitly (it does today).
- 2026-06-06 [run-next-phase/P53] Any phase that adds an action verb or a node field must treat the **schema** as an implicit deliverable: the new verb/field is silently stripped by Zod (`contracts.ts` enum + `node-definitions.ts` node schema) until both are widened — the symptom (`Invalid option` in the node-red log, command never reaches the client) reads like a client bug.
- 2026-06-01 [run-next-phase/P33] Runtime limitation: `buildAppSnapshot` (nodes/webapp.js) picks per-client state OR broadcast state but never MERGES them — once a client has any per-client state, broadcast updates go invisible to it. (The fresh-clientId-per-load half was since fixed by P87's persistent clientId; the merge gap remains.) Making per-client apps fully viable needs broadcast merged under per-client state. Not yet phased.
- 2026-06-09 [run-next-phase/P104] Pre-existing E2E red baseline on develop, unrelated to the phase: `tests/e2e/nodes/view/ui-alert.spec.ts` (3 tests: duration auto-hide, countdown attr, editor countdown+duration valid), `tests/e2e/p36-app-shell.spec.ts` (app-bar-title), and a flaky `tests/e2e/p106-deploy-live-delivery.spec.ts` (topology reload). Confirmed they fail with the renderer reverted to the pre-P104 commit, so they are not caused by P104. The orchestrated sub-agent cannot close out a phase against a red E2E baseline per the anti-baseline rule, yet it also must not fix unrelated phases — these need an orchestrator-level decision / a dedicated fix-bug pass before the suite is green.
- 2026-06-10 [run-next-phase/P112] In a worktree, the CLAUDE.md project-instructions context block embeds the SHARED-checkout absolute paths (e.g. /Users/.../node-red-contrib-webapp/nodes/webapp.js, not the .claude/worktrees/<id>/ copy). Reading those paths "works" (identical content) but the first Edit fails with an isolation error and the file then "has not been read yet" because the read tracked the wrong path — costing a re-read per file. Worth a one-line note in the orchestrated section: in a worktree, always operate on the .claude/worktrees/<id>/ path, ignore the shared paths the context prints.

- 2026-06-10 (P114): worktree handed me the orphan base (HEAD at `db4f4ff "initial checkin"`, tree missing `packages/`, `.ai/`, `docs/`) again — the self-heal `git switch -C phase/P114 develop` worked, but this is the same provisioning bug logged 15+ times. The run-next-phase prompt already documents it; flagging another occurrence for the count.
- 2026-06-10 [run-next-phase/P118] Worktree handed the orphan base again (HEAD at `db4f4ff`, no `packages/`); self-heal `git switch -C phase/P118 develop` + `pnpm install && pnpm build` worked. Same provisioning bug — count++. Also: the E2E Node-RED on this host (port 1882, the `node-red` on PATH, v3+) makes `RED.util.evaluateJSONataExpression` ASYNC-ONLY (throws "must include a callback"; even native `expr.evaluate()` returns a Promise). So a navigate `to`/param of type `jsonata` cannot be resolved SYNCHRONOUSLY in `buildActionCommand` — it silently yields undefined (P66's jsonata-navigate was already broken on this NR; no E2E covered it). P118 keeps jsonata support in code (works on older NR + unit-tested with a mock) but its url-mode E2E uses a `msg`-typed `to` instead. Fixing jsonata navigate properly needs an async refactor of the action-command path (out of P118 scope).

- 2026-06-10 (P113): In a worktree, the Read tool's earlier reads resolved to the SHARED checkout path while Write/Edit are pinned to the worktree — a heredoc `cat > .ai/...` with a worktree-relative path failed silently because the shell cwd resets to a non-worktree dir between Bash calls. Always use the absolute worktree path for file redirection in Bash, and Read the worktree copy (not the shared checkout) before editing.

- 2026-06-12 (P155): `grep` over `nodes/webapp.js` returns nothing without `-a` — the file contains a byte that trips grep's binary heuristic, so plain `grep "tabs"` silently reports no matches (exit 1). Cost a few minutes chasing a phantom "ui-tabs isn't in webapp.js". Use `grep -a` on webapp.js. Also: eslint `no-fallthrough` flags a multi-line comment block placed *between* two empty `case` labels in renderer.ts (it must end in `// falls through` or sit above the whole group) — moved the P155 tabs comment above the `case "select":` group to fix.

## P168 (ui-tabs/ui-tab renderer+editor)
- The `node-red-node` skill's mapConfig template and `getUiId` use `config.id` as the canonical component id, but several tab fixtures/tests carry a separate `uiId`. For a node whose **id is a semantic key** (ui-tab id = slot key + activeTab token), the test harness's `{...mapConfig(n), id, uiId}` override silently diverges the component id from the mount target. Worth a one-line note in the skill: the component id is `config.id`; mount keys must use that same id.
- ADR 0018/P168 contract says children mount `ui-tabs:<id>/content`, but the editor mount-tree picker only emits the generic `container:<id>/content` form. P168 resolved this by aliasing `container:` in the renderer's tab matcher — but the contract docs never mention the alias, which cost some debugging. The two mount shapes being interchangeable for tabs should be stated in ADR 0018 or the ui-tabs spec.

## P178 (ui-component renderer)
- `packages/renderer/src/renderer.ts` contains a NUL byte (around offset ~16767), so plain `grep`/`rg` report it as a "binary file" and print no matches — the P178 prompt's "grep `expandRepeat`/`itemScope` in renderer.ts" silently returns nothing and looks like the symbols are absent. Had to fall back to `rg -a` (treat-as-text) to locate them. Worth either stripping the stray NUL from renderer.ts or noting in the prompt/context-budget that this file needs `rg -a`.
- 2026-06-19 (P195): Spec field-tables embed several non-field code-spans — sub-tables (item-object schemas `| Feld | Typ | …`, enum-value tables `| Wert | …`), struck-through removed-field rows, and typedInput binding-carrier twins (`<base>Binding`/`<base>Path`). A naive "code-spans in the Felder section" parser yields a false-positive wave. The robust contract parser must header-gate to the canonical `| Feld | Label | Editor-Typ | Pflicht | Beschreibung |` table, drop `~~…~~` rows, and treat carriers as boilerplate. Worth noting in the spec-format conventions so future field-table authors keep that shape.

## P208 (ui-list item-field mapping)
- 2026-07-12: The lane list scoped ui-list P208 to `packages/schema`, serializer, ui-list.html, webapp.js, docs, tests — but NOT `packages/editor`. Adding the four `.default()` fields to `uiListNodeDefinitionSchema` makes them REQUIRED in the z.infer OUTPUT type, so the editor's `createDefinition("ui-list", …)` literal (`packages/editor/src/nodes.ts`) no longer type-checks and `pnpm build` fails. Fixing it required a small, in-pattern edit to editor nodes.ts (mirror displayValue/badgeVariant) that was technically out of the drawn lane. A schema field addition with `.default()` will always ripple into the editor definition literal — future ui-* field-mapping lanes should include `packages/editor/src/nodes.ts`.

## ADR 0032 (store sub-path missing-key bugfix)
- 2026-07-13: The stray NUL byte in `renderer.ts` flagged by the P178 entry above was STILL present (exactly 1, inside the `store-subpath ${message}` dedup-key template) AND `nodes/webapp.js` had the same corruption (1 NUL in the `onReactiveError` dedup key). Plain `grep`/`rg` treat both files as binary, so locating the error string `Store "…": Pfad "…" nicht gefunden` failed silently — only `git grep` found it. Cost ~10 min. Removed both NULs (→ space) as part of this fix; consider a read-only tripwire (`grep -rlP '\x00'` over `packages/**/src` + `nodes/`) so a NUL byte can never re-enter and quietly break search again.
- 2026-07-13: The root `pnpm vitest run <path>` globs `.claude/worktrees/agent-*/` — a leftover phase/P214 worktree — and runs STALE duplicate copies of the same test file alongside the real one (two entries for `p131-store-subpath.test.ts`, one old/passing, one live). Confusing when debugging a failing test. Per-package `pnpm --filter … test` is unaffected. Worktrees left behind by an orchestrated run should be pruned, or the root vitest `include`/`exclude` should drop `.claude/worktrees/**`.
- 2026-07-17 (P240): A fresh orchestrated worktree has NO `node_modules` and NO `packages/*/dist`, so `pnpm validate` fails twice in a row with misleading errors before it can ever be green: first `check:roadmap` dies on `ENOENT … node_modules/.pnpm` (it scans the store to load js-yaml), then 94/110 runtime test FILES fail to collect with `Cannot find module '../packages/schema/dist/index.js'`. Neither error says "run install + build first" — both read like a broken tree. Cost ~5 min per worktree agent, every time. The orchestrated prompt should state the worktree bootstrap (`corepack pnpm install && pnpm build`) as step 0, or `validate` should fail fast with that hint.

## P239 (ui-icon icon name bindable in the editor)
- 2026-07-17: **The NUL-byte-in-source problem is NOT limited to `renderer.ts`/`webapp.js`** (see the 2026-07-13 entries): `resources/lib/webapp-client.js` is also `file`-detected as `data`, so plain `grep -rn "sl-tab-show" resources/lib/` returns **nothing at all** — not even "Binary file matches" when `-n`/pipes are in play. Chasing "who listens for `sl-tab-show`?" I concluded *no listener exists in the repo* and built a whole (wrong) hypothesis on it; the handler was sitting at `webapp-client.js:835` the entire time. `grep -a` (or `git grep`) finds it instantly. This silently-empty grep result is far more dangerous than a noisy error. The tripwire suggested on 2026-07-13 (`grep -rlP '\x00'` over `packages/**/src` + `nodes/`) is still not implemented and should also cover **`resources/lib/`** — cost ~15 min here.
- 2026-07-17: `.ai/agents/validation.md`'s anti-baseline rule ("fix every failing test, no exceptions, pre-existing is not a valid reason") collides with the orchestrated brief ("work only on your phase; don't scope-creep") when the one red test belongs to **another node**. Worse, the failure I found (ui-tabs E01) was explicitly waved through by **P170's own result note** as "a confirmed flake — re-ran 14/14 green", so the anti-baseline rule had already been rationalised away once, in writing, in a closed package. It was not a flake: it is a deterministic race (the tab group's own default-tab `sl-tab-show` → a real `change` POST) that passes alone and fails in-file. Two suggestions: (a) a `## Result` must not be allowed to close a phase with "confirmed flake" unless the flake is *diagnosed* (a re-run that goes green is not a diagnosis); (b) the orchestrated prompt should say explicitly whether a sub-agent may drive-by-fix an unrelated red test, and if so, in a separate commit — I did the latter and flagged it for the orchestrator.
