node_modules/
dist/
coverage/
*.tsbuildinfo
.node-red-dev/
.node-red-e2e/
.node-red-example/
test-results/
playwright-report/

# Vendored Shoelace cdn build (ADR 0008 / P63) — generated by
# scripts/vendor-shoelace.js on `pnpm build` and npm `prepare`; shipped in the
# npm tarball via the package.json `files` field ("resources").
resources/shoelace/

# Agent-OS per-run cost trail (AGENTS.md rule 10). Append-only, written by the
# SessionEnd/SubagentStop hook. Local data — gitignored to avoid EOF-append
# merge churn. The recorder script + README under .ai/hooks/ ARE tracked.
.ai/agent-runs.jsonl


# agent phase worktrees
.claude/worktrees/
