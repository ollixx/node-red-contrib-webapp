# Epic: ui-skeleton

Per-node work packages for `ui-skeleton`.

> Goal: `ui-skeleton` erfüllt Spec — `visible` als Standard-typedInput (kein
> Sonderweg `visiblePath`), N/A-Felder kontextabhängig disabled.
