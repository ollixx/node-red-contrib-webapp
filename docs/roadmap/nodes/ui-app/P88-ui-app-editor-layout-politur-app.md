---
id: P88
title: "ui-app Editor-Layout-Politur — App-URL-Link hinter das Label rücken, bündig mit den anderen Feldern (2. Spalte); Logging-Select bündig in die 2. Spalte; Info-Button HINTER die SelectBox"
epic: nodes/ui-app
status: done
dependencies: [P61]
node: ui-app
spec: docs/nodes/structure/ui-app.md
tests: tests/e2e/nodes/structure/ui-app.tests.md
---
# P88 — ui-app Editor-Layout-Politur — App-URL-Link hinter das Label rücken, bündig mit den anderen Feldern (2. Spalte); Logging-Select bündig in die 2. Spalte; Info-Button HINTER die SelectBox

## Result

**Delivered:** Polished the ui-app editor HTML layout: App URL link placed directly in the second column (no padding-top wrapper div); Logging select moved to a plain label with the info button placed AFTER the select instead of inside the label markup.

**Stats:** 1 HTML file changed (nodes/structure/ui-app.html), 1 test catalogue note updated (ui-app.tests.md); 558 unit tests pass; no schema/runtime changes

**Notes:** Pure editor HTML layout polish — no fields, no runtime behaviour, no schema changes.

**Cost:** session ae07df7, 5m
