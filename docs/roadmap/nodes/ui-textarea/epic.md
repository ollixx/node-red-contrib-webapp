# Epic: ui-textarea

Per-node work packages for `ui-textarea`.

> Goal: `ui-textarea` erfüllt seine Spec und die Binding-Ubiquität (ADR 0012) —
> `value` als kanonischer typedInput, bindbares `disabled`.
