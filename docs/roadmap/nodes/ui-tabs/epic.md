# Epic: ui-tabs

Per-node work packages for `ui-tabs`.

> Goal: `ui-tabs` — aktiver Tab als zweiseitiges `value`-Binding; die
> Tab-Definition (`tabs`) und dynamische Slots sind separat (Collections /
> [P142]).
