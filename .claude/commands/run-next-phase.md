---
description: Implement the next ready roadmap phase, test-first, then close it out.
argument-hint: [phase id — optional; otherwise the next ready one]
---
Read `.ai/prompts/run-next-phase.prompt.md` in full and execute it **exactly** — the setup read order, the green-baseline + test-first protocol, the validation steps, and the close-out (Result + status + INDEX + `pnpm check:roadmap`). Do not skip steps.

Additional context for this run (e.g. a specific phase id): $ARGUMENTS
