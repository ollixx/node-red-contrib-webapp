---
description: Run the 3-step validation protocol on a phase (no implementation changes).
argument-hint: [phase id — optional; otherwise current/last phase]
---
Read `.ai/prompts/validate-phase.prompt.md` in full and execute it **exactly** — the three-step protocol in `.ai/agents/validation.md` against the phase's `acceptance` list. Verify only; make no implementation changes.

Phase to validate (optional): $ARGUMENTS
