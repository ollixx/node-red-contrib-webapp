# Testkatalog: ui-pagination

> Format gemäß `.ai/agents/node-testing.md`. Angelegt mit P154 (Field-Typing Welle 2).

## Geplante Testziele (P154)
- `total` als Wert-typedInput (lesende Quelle).
- `currentPage` zweiseitig (Store lesen+schreiben) + page-change-Event.
- Pfad-Migration (`totalPath`/`currentPagePath` → state-Binding).
