# Testkatalog: ui-menu

> Format gemäß `.ai/agents/node-testing.md`. Angelegt mit P157 (Field-Typing Welle 2).

## Geplante Testziele (P157)
- `items` als Wert-typedInput (Array-Quelle aus Store/Query) rendert die Einträge.
- `activeRoute` markiert den aktiven Eintrag aus einem Binding.
- Pfad-Migration.
